<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_centerHorizontal="true"
    android:layout_centerVertical="true"
    android:background="@android:color/white"
    >

    <TextView
        android:id="@+id/textViewSourceDuration"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="@android:color/black"
        android:gravity="center"
        android:text="20 Min"
        android:textColor="@android:color/white"
        android:textSize="12sp"
        />

    <TextView
        android:id="@+id/textViewSourceLocationName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toRightOf="@id/textViewSourceDuration"
        android:gravity="center"
        android:maxLines="1"
        android:maxLength="10"
        android:ellipsize="marquee"
        android:padding="5dp"
        android:text="Ludhiana"
        android:textAlignment="center"
        android:textColor="@android:color/black"
        android:textSize="12sp"
        />

    <ImageView
        android:id="@+id/imageViewSouceNextArrow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toRightOf="@id/textViewSourceLocationName"
        android:background="@android:color/white"
        android:src="@drawable/ic_arrow_next" />

</RelativeLayout>